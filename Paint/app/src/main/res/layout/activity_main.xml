<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/settings_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="0"
        android:orientation="horizontal"
        android:layout_margin="8dp">
        <TextView
            android:id="@+id/pen_preview"
            android:layout_width="36dp"
            android:layout_height="36dp"
            app:layout_constraintStart_toStartOf="@id/settings_layout"
            app:layout_constraintEnd_toStartOf="@id/pen_change_button"
            app:layout_constraintTop_toTopOf="@id/settings_layout"
            app:layout_constraintBottom_toBottomOf="@id/settings_layout"
            android:gravity="center"
            android:text="6"
            android:textColor="#ffffff"
            android:textSize="18sp"
            android:background="@drawable/circle"
            android:backgroundTint="#000000"/>
        <Button
            android:id="@+id/pen_change_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/pen_preview"
            app:layout_constraintEnd_toStartOf="@id/clear_canvas_button"
            app:layout_constraintTop_toTopOf="@id/settings_layout"
            app:layout_constraintBottom_toBottomOf="@id/settings_layout"
            android:text="Change pen"/>
        <Button
            android:id="@+id/clear_canvas_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/pen_change_button"
            app:layout_constraintEnd_toEndOf="@id/settings_layout"
            app:layout_constraintTop_toTopOf="@id/settings_layout"
            app:layout_constraintBottom_toBottomOf="@id/settings_layout"
            android:text="Clear"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
    <View
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="#000000"/>
    <FrameLayout
        android:id="@+id/canvas_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</LinearLayout>